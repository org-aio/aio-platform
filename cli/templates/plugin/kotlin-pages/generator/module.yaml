apply: [//network.module-template.yaml]

product: jvm/app

description: "Generate the installable AIO PageDefinition artifact"

dependencies:
  - //model

settings:
  kotlin:
    version: 2.3.21
  jvm:
    jdk:
      version: 25
      selectionMode: javaHome
    release: 17
    mainClass: __PACKAGE_NAME__.MainKt
