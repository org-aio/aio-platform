# 全栈模板

三种语言默认生成前端、后端、共享模型以及自动交付配置。构建入口同时执行测试，产物统一交给宿主二进制插件协议。

TypeScript 可通过 `--framework nuxt` 或 `--framework next` 使用原生框架服务端。`web/` 是两种框架共用的模型、服务、通信客户端、服务端归档和接口测试；生成时先展开共用层，再展开框架目录。默认 TypeScript 模板仍使用原生浏览器和 Node。
